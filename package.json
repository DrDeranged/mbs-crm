{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "seed:lenders": "node lib/db/seed-lenders.mjs",
    "test": "pnpm --filter @workspace/api-server run test && pnpm --filter @workspace/mbs-crm run test && pnpm --filter @workspace/scripts run test && pnpm --filter @workspace/scripts exec tsx --test ../lib/db/src/*.test.ts",
    "lint:migrations": "pnpm --filter @workspace/scripts run lint:migrations",
    "check:schema": "pnpm --filter @workspace/scripts exec tsx ./src/check-schema.ts",
    "smoke": "playwright test --config playwright.smoke.config.ts",
    "migrate:rehearse": "pnpm --filter @workspace/scripts exec tsx ./src/migrate-rehearse.ts",
    "preflight": "pnpm --filter @workspace/scripts exec tsx ./src/preflight.ts",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  }
}
