INTEGRATION + ROBUSTNESS CYCLE — two workstreams. tsc clean per workstream, push
after each, publish once at the end, verify on the deployed site.

════════════════════════════════════════════════════════════════════
WORKSTREAM A — CRM INTEGRATION (the site becomes a measurable funnel)
════════════════════════════════════════════════════════════════════
1. CALCULATOR → APPLICATION HANDOFF: the calculator's "Get your real offer" CTA (and
   the home mini-calc's apply CTA) now carry the user's current inputs:
   https://app.my-business-solutions.com/apply?amount={amount}&term={term}&freq={freq}&src=calculator
   (the CRM's /apply already accepts params after its parallel build cycle — pass
   them; do not validate on our side beyond URL-encoding).
2. SOURCE ATTRIBUTION SITEWIDE: every CTA to the apply URL gains a src param
   identifying its origin: src=hero, src=header, src=products, src=cta-band,
   src=calculator, src=mini-calc, src=blog-{article-slug}, src=contact-page,
   src=get-started-redirect (update the /get-started redirect target accordingly).
   One helper builds these URLs — no hand-typed duplicates.
3. MBSASSIST LEAD CAPTURE: when a conversation reaches 2+ user messages, MbsAssist
   may offer once (not repeatedly): "Want us to reach out? Share your name and email
   and a specialist will follow up." If the visitor provides name+email (optional
   phone) in the panel's small capture form (NOT free-text parsing), POST to
   https://app.my-business-solutions.com/api/leads/capture/elementor with flat keys
   { name, email, phone, message: "MbsAssist inquiry" } — reuse the contact form's
   exact posting code. Show a friendly confirmation. Never collect SSN/financial data
   in chat; the assistant's system prompt already forbids it — keep that intact.
4. FOOTER: add "Check application status" linking to
   https://app.my-business-solutions.com/apply/status
5. VERIFY on deployed: calculator handoff lands on /apply with values prefilled (test
   with the CRM live), a src param rides every apply CTA (spot-check 5), MbsAssist
   capture posts and the lead appears in the CRM (then delete the test lead), status
   link resolves.

════════════════════════════════════════════════════════════════════
WORKSTREAM B — ROBUSTNESS & PERFORMANCE PASS (zero visual changes)
════════════════════════════════════════════════════════════════════
1. CODE-SPLITTING: React.lazy + Suspense for all routes except Home (light centered
   spinner fallback). MbsAssist panel lazy-loads on first open (pill stays eager).
   Report Home's initial JS before/after.
2. FONTS: self-host Sora + Inter via @fontsource (only used weights), remove Google
   Fonts requests, font-display: swap.
3. ACCESSIBILITY: meaningful alt on every image (alt="" for decorative); aria-labels
   on icon-only buttons (menu, Ask MBS, steppers); marquee aria-hidden with sr-only
   industry list; one h1 per page; labeled form inputs; focus-visible rings
   everywhere; reduced-motion re-verified. Target Lighthouse Accessibility 100.
4. RESILIENCE: width/height on images (no CLS) + loading="lazy" below the fold;
   poster fetchpriority="high"; MbsAssist network failure → friendly retry state;
   contact form one silent retry before showing the retry message.
5. PROVE IT: Lighthouse (deployed, after publish) Home + Calculator, mobile+desktop —
   report all scores before/after. Targets: Perf ≥90, A11y 100, Best Practices ≥95,
   SEO 100. Full QA matrix re-run — all PASS.