CRM VISUAL UNIFICATION — restyle the MBS CRM to share the website's "Momentum" design
language. SURFACE TREATMENT ONLY: zero functional, routing, schema, or logic changes.
Tokens: navy #1F4E79, ink #0E2A47, paper #FFFFFF, cloud #F5F8FB, slate #46586C,
go #17A567, line #DCE4EC. Fonts: Sora (headings/page titles, 600/700) + Inter (body,
UI) via same loading pattern as the site; tabular-nums on ALL numbers, currency, KPIs.

1. SHELL: sidebar goes ink #0E2A47 — white logo treatment (white chip if needed), nav
   items #EAF1F8 at 70%, active item = white text + 3px go-green left rail + navy/40
   pill bg; section labels 11px uppercase letterspaced. Top bar stays white with a 1px
   line border; page titles in Sora.
2. BUTTON SYSTEM (port exactly from the site): primary = gradient green #1DB674→#149258,
   1px white/15 inner top highlight, hover lift+glow (0 8px 24px rgba(23,165,103,.35)),
   press .98; pill radius, 44px height in-app. Secondary = 1px line border, green text/
   border on hover. Destructive stays red but same shape system. Focus = 2px go ring
   offset 2px, everywhere.
3. CARDS & TABLES: cards 14px radius, 1px line border, white/40 top-edge highlight,
   hover lift-2px where interactive. Tables: header row cloud bg + slate 12px uppercase
   labels; row hover cloud; status pills adopt the site's gradient-green "Matched" pill
   recipe (green=positive, navy=neutral, amber/red unchanged for warning/danger).
4. KPI DASHBOARD: stat numbers in Sora tabular with the site's CountUp on first mount;
   go-green accent for positive deltas; each stat card gets the top-edge highlight.
5. FLOATERS/MODALS: dialogs 14px radius with the glass recipe on overlays (ink/40
   backdrop-blur); toasts/notification popovers use the site's glass treatment (ink/70
   blur, white/15 border). The notification bell dot = go-green pulse.
6. LOGIN PAGE: this one CAN go cinematic — ink background, mesh gradient, glass card,
   white logo, gradient-green sign-in button. The brand handshake moment.
7. Preserve ALL existing spacing/layout/information density — this is a reskin, not a
   relayout. Respect prefers-reduced-motion on every animation.
Screenshot dashboard, leads list, lead detail, login (desktop) → verify against the
website's aesthetic. tsc clean, push. Do NOT republish — I review screenshots first.