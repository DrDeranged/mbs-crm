COMPLETE CRM UPDATE CYCLE — four workstreams in order. Report a commit hash per
workstream. tsc clean per workstream, push per workstream, publish ONCE at the end,
then run the verification section. No email/SMS sending anywhere. Migrations additive
only.

════════════════════════════════════════════════════════════════
WORKSTREAM 1 — POST-AUDIT CORRECTIONS (fix the audit's findings)
════════════════════════════════════════════════════════════════
1A. SLUG BACKFILL (production): the slug column shipped but the backfill never ran —
    all 3 production users have null slugs. Execute the backfill through the
    production-safe migration/publish path (never raw workspace DB access): active
    users get slugs from first name, collision rule = append last-initial. There are
    two "Arslan Din" admin users (id 7 and 12): assign slug "arslan" to the PRIMARY
    (the one owning existing leads/activities — report which), flag the other as
    duplicate for review — do NOT delete it. Nate → "nate". Report final id→slug
    table.
1B. RESERVED SLUGS: Calvin Tuon and Rahmare Davis haven't signed in yet (no user
    rows). Implement slug reservation on user auto-creation: email matches
    calvintuon@gmail.com OR calvin@my-business-solutions.com → slug "calvin";
    rahmaredavis@gmail.com OR rahmare@my-business-solutions.com → "rahmare". Until
    they sign in, /r/calvin correctly serves the generic fallback.
1C. CALCULATOR PREFILL BUG: /apply?amount=150000 does not populate Requested Amount;
    term/freq don't land. Diagnose the actual control types and map incoming values:
    amount → the amount field or nearest band; term (months) → term control; freq
    (daily|weekly|biweekly|monthly) → frequency control. Snap sane values to nearest
    allowed option; ignore garbage silently. Verify with
    /apply?amount=150000&term=24&freq=weekly&type=equipment (screenshot).
1D. CEO TEMPLATE SEED (production): run the template seed once through an
    authenticated/production-safe path; both templates ("Quick Question About
    Business Funding", "Fast Business Funding Options – Same Day Approval") must
    exist with zero smartyapp/typeform URLs in bodies. Run the seed AGAIN and report
    both counts identical (idempotency proof).

════════════════════════════════════════════════════════════════
WORKSTREAM 2 — DEAL PIPELINE (the centerpiece; modeled on the CEO's real tracker
and his founding spec's status/KPI requirements)
════════════════════════════════════════════════════════════════
A deal/opportunity layer ON TOP of leads — do NOT mutate lead statuses. Leads and
deals are separate: a prospect is a lead; an active funding pursuit is a deal,
optionally linked to a lead.

2A. SCHEMA: deals table — id, leadId (nullable FK), dealName, stage, amount
    (dollars, integer), approxGm, actualGm (dollars, nullable), assignedTo (user
    FK), createdAt/updatedAt, fundedAt (nullable timestamp, set when stage becomes
    funded), isArchived. Activities gain optional dealId. Stage enum EXACTLY
    (ordered): waiting_on_app, information_needed, submitted, approved,
    going_to_funding, in_funding, funded, declined, dead, hold_on.
2B. UI: "Deals" in the sidebar → kanban board by stage (drag between stages,
    Momentum-styled, amount + GM on cards, rep avatar) with a table-view toggle.
    Deal detail page: all fields editable, linked lead (two-way jump), activity
    timeline, stage history (log every change: from, to, user, timestamp).
    "Convert to deal" action on lead detail (prefills, links). Setting stage to
    funded prompts for actualGm and stamps fundedAt.
2C. GM DASHBOARD: dashboard gains a Deals row — KPIs: Funded GM (sum actualGm,
    funded), Awaiting GM (sum approxGm, active stages), Pipeline Value (sum amount,
    active stages), Avg Funding Time (mean fundedAt−createdAt of funded deals; show
    "—" until data exists), deals-by-stage mini bar, and a Rep Performance strip
    (per rep: active deals, funded count, funded GM). Existing lead KPIs unchanged.
2D. PERMISSIONS: reps see own deals; manager/admin see all. Pending gate applies.
2E. SEED THE REAL PIPELINE idempotently (match on dealName; amounts in dollars,
    $1MM=1000000; where a note is given, add it as the deal's first activity):
    Fastgrass Hydroseed LLC|submitted|65000|8000||Customer held out another month
    Heartlands Entertainment LLC|submitted|250000|20000||Awaiting Banks
    University of Illinois|in_funding|365000|12000|12000|In Funding
    Browns Farm|information_needed|40000|||Ghosted
    Emerald Hydroturf|information_needed|65000|||Awaiting App/Banks/Quote
    Frisco Station Arcade|dead||||Lost deal, Declined
    Bryce Roder dba SETX Hydroseed|dead||||Did not need
    Jim (Moss Deal)|hold_on|120000|||
    Wyatt (Kincaid Deal)|dead|123000|||
    Integrity Outdoor Services|submitted|65000|12000|6000|Docs In, customer ghosted
    Jared Yost dba Tribal AG|approved|225000|22000|12000|Waiting on docs from CPA
    Cornell University|approved|504000|15000|15000|Waiting on signed proposal
    Talya Friend|dead|40000|4000||Tesla Chargers, waiting on App and Banks
    Diamond AG|going_to_funding|360000|25000||Waiting SOS
    Oregon Hydroseed|submitted|116000|10000||May want to hold off, trying better option
    AM Global Investments|declined|10000|2000||
    Slick City Water Park|waiting_on_app|1000000|40000||Spoke to Pat, doing app and quotes
    5 Boys Moving|declined|36000|3000||sent quote
    Mitchell & Vereen Transportation LLC|funded|30000|||Calvin's deal → assign
    reserved-rep calvin if user exists else admin; same for the next three:
    Four Pillars|in_funding|118000|||Calvin's deal
    Antleys|submitted|36000|||Calvin's deal
    R2Muse Trucking|submitted|37000|||Calvin's deal
    Erosion Specialist|funded|17000|2300|2300|Booked
    TP K1 Speed|funded|1500000|35000|25500|Funded
    Jared Yost dba Tribal AG (funded tranche)|funded|110000|9000|8800|Booked
    Unassigned → primary admin. For funded seeds, fundedAt = seed date.

════════════════════════════════════════════════════════════════
WORKSTREAM 3 — FLYER LIBRARY INGEST (only if /flyer-uploads/ exists in the
workspace; if absent, report SKIPPED and continue)
════════════════════════════════════════════════════════════════
Ingest approved flyers from /flyer-uploads/ into the flyer/marketing system: proper
naming, visible in the marketing UI, downloadable by reps. Do not modify artwork.
List malformed/duplicate files and skip them. Delete /flyer-uploads/ after.

════════════════════════════════════════════════════════════════
WORKSTREAM 4 — PUBLISH + VERIFICATION (production, evidence per item, delete all
test records after capture)
════════════════════════════════════════════════════════════════
Publish once, confirm build time postdates all commits, then:
V1. id→slug table: all active users slugged; duplicate-admin flagged not deleted.
V2. /r/nate (or another real slug) renders PERSONALIZED (screenshot); /r/notreal
    renders generic; valid-vs-invalid responses don't leak slug existence.
V3. Test application via a real /r/{slug} equipment path → lead auto-assigned to
    that user, source qr-card, slug recorded (dump fields). Delete.
V4. /apply?amount=150000&term=24&freq=weekly&type=equipment prefills all four
    (screenshot); garbage params ignored.
V5. QR PNG+SVG for one slugged user; decode PNG → exact /r/{slug} URL.
V6. Both CEO templates in production, bodies clean, seed idempotent (counts).
V7. Deals board renders all ~25 seeds in correct stages (screenshot). Report
    computed KPIs: Funded GM (expect 36600 from actuals 25500+8800+2300), Awaiting
    GM, Pipeline Value. Drag one deal between stages → stage history logged; drag
    back. Convert one lead to a deal → verify link both ways → delete it.
V8. Equipment zero-statement application completes; WC skip-to-rep creates the
    activity + rep task. Delete all test records.
V9. Security: /api/leads, /api/users, /api/analytics/summary 401 unauthenticated;
    /r/:slug 200; pending gate intact.
V10. Health: healthz+deep OK, error_log 24h count, no new 5xx, jobs green, 521
    leads + 4 lenders untouched, dashboard lead KPIs unchanged.
FINAL: one table — every V-item PASS/FAIL/BLOCKED + evidence + all workstream
commit hashes. Fixes allowed ONLY for items failing their own spec above; one
re-publish max; re-verify fixed items.