CRM CRAFT PASS — the restyle applied tokens but not craft. This pass builds the detail
work that makes it feel like the marketing site's sibling. SURFACE ONLY — zero logic/
routing/schema changes. All motion transform/opacity, reduced-motion safe. No republish.

1. SIDEBAR (the signature surface): ink #0E2A47 with a barely-visible noise texture
   (3%) and a 1px white/8 right border. Nav items: 40px rows, 8px radius hover (white/5
   bg, 150ms), active = navy/50 pill + 3px go-green left rail that ANIMATES height-in
   on change (200ms) + icon shifts white. Section labels: 10px, white/40, 0.1em
   tracking, 24px top margin. User card at bottom: glass chip (white/5, blur, white/10
   border) with avatar ring in go-green. Collapse behavior untouched.

2. TOP BAR: white, 1px line bottom; on content scroll, gain soft shadow (200ms). Page
   title in Sora 600 with a 2px go-green underline accent (24px wide, left-aligned
   under the first word). Notification bell: go-green dot with 2s pulse; its popover =
   glass (ink/70 blur, white/15 border, white text).

3. DASHBOARD KPIs: each stat card gets the top-edge highlight, a tiny 11px uppercase
   slate label, Sora tabular number with CountUp on mount, and a subtle go-green
   spark-underline that draws in (300ms, staggered 80ms per card). Positive deltas =
   go-green pill with ↑; the cards lift-2px on hover.

4. LEADS TABLE: header row cloud bg, 11px uppercase slate letterspaced; row height
   52px; hover = cloud + 2px go-green left edge; status pills = the site's gradient
   pill recipe sized 24px (green=active/positive, navy=neutral, amber=attention,
   red=danger); lead scores render as a 32px radial ring (go-green arc, slate track,
   tabular number inside). Empty states: centered illustration-free message in Sora +
   one-line slate copy + primary gradient CTA — no bare "No data" text anywhere in the
   app.

5. LEAD DETAIL: header block gets a cloud band with the business name in Sora 600,
   status pill, and the assigned-rep chip (avatar + name in a line-border pill). Tabs:
   the site's underline-draw treatment (2px go-green, slides between tabs 200ms).
   Activity feed: timeline with 8px go dots and hairline connector. AI briefing card:
   ink background, white text, glass inner sections, gradient-green Generate button —
   make the AI feature FEEL premium.

6. TRANSITIONS: route changes = 150ms fade+rise 8px. Dialogs: scale .97→1 + overlay
   ink/40 blur-in. Toasts: glass recipe, slide-in from top-right with spring.

7. LOGIN: verify the cinematic treatment actually landed — ink bg + radial mesh
   (navy→ink), noise at 4%, centered glass card (white/8, blur 16, white/15 border,
   20px radius), white-chip logo, Sora heading, gradient-green full-width button with
   sheen on hover. If it reads flat, rebuild to this spec.

8. CONSISTENCY SWEEP: kill every remaining default-blue link, default shadow, default
   focus outline; all interactive elements get the 2px go focus ring; all numbers
   app-wide tabular. Border radii: 8px inputs, 14px cards, pill buttons — no other
   values.

Screenshot dashboard, leads, lead-detail, login → commit to /docs/screens/ in the repo.
tsc clean, push, NO republish.