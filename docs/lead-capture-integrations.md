# Lead capture integrations

The legacy Elementor capture endpoint was retired after the DNS cutover.
Requests to that endpoint now return **404 Not Found**.

Use the supported public application form (`/apply`) or the existing website
lead-capture API (`POST /api/leads/capture`) for new integrations.